- [x] `src/mock/data.ts` 提供完整的章节辅助函数（上一章、下一章）。
- [x] `Reader.tsx` 正文能正确显示对应 `bookId` 和 `chapterId` 的文本。
- [x] `Reader.tsx` 提供调整字号、行距、阅读主题的功能，并即时生效。
- [x] `Reader.tsx` 页面可以正确打开目录侧边栏并跳转到指定章节。
- [x] 用户翻页或打开某章时，能自动同步保存进度到 `bookshelf` 中。
- [x] `Bookshelf.tsx` 页面正确显示已添加到书架的所有书籍，包括上次阅读的章节名。
- [x] 用户在 `Bookshelf.tsx` 点击“继续阅读”按钮能准确跳转到上次阅读的章节。
- [x] 用户在 `Bookshelf.tsx` 点击“移出书架”按钮能正确移除书籍。
